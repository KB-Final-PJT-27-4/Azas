<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.azas.domain.finance.goal.mapper.FinancialGoalTemplateMapper">

    <select id="findActiveDefaultTemplates"
            resultType="com.azas.domain.finance.goal.entity.FinancialGoalTemplate">
        SELECT
            financial_goal_template_id,
            goal_name,
            description,
            icon_key,
            sort_order
        FROM financial_goal_template
        WHERE is_default = 1
          AND is_active = 1
          AND created_by_member_id IS NULL
        ORDER BY sort_order ASC, financial_goal_template_id ASC
    </select>
</mapper>
