<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.azas.domain.auth.mapper.SocialAccountMapper">

    <resultMap id="socialAccountResultMap"
               type="com.azas.domain.auth.entity.SocialAccount">
        <id property="socialAccountId" column="social_account_id"/>
        <result property="memberId" column="member_id"/>
        <result property="provider" column="provider"/>
        <result property="providerSubject" column="provider_subject"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByProviderAndSubject"
            resultMap="socialAccountResultMap">
        SELECT
            social_account_id,
            member_id,
            provider,
            provider_subject,
            created_at,
            updated_at
        FROM social_account
        WHERE provider = #{provider}
          AND provider_subject = #{providerSubject}
    </select>

    <insert id="insert"
            parameterType="com.azas.domain.auth.entity.SocialAccount"
            useGeneratedKeys="true"
            keyProperty="socialAccountId"
            keyColumn="social_account_id">
        INSERT INTO social_account (
            member_id,
            provider,
            provider_subject
        ) VALUES (
                     #{memberId},
                     #{provider},
                     #{providerSubject}
                 )
    </insert>

</mapper>