<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:task="http://www.springframework.org/schema/task"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/task
           https://www.springframework.org/schema/task/spring-task.xsd">
    <task:annotation-driven
            scheduler="assetReportTaskScheduler"/>

    <task:scheduler
            id="assetReportTaskScheduler"
            pool-size="1"/>
    <context:component-scan
            base-package="com.azas.domain,com.azas.global.security">
        <context:exclude-filter
                type="annotation"
                expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter
                type="annotation"
                expression="org.springframework.web.bind.annotation.RestController"/>
    </context:component-scan>

    <!-- OAuth 제공자 REST API 통신 -->
    <bean id="oauthRestTemplate"
          class="org.springframework.web.client.RestTemplate"/>

    <bean id="oauthObjectMapper"
          class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <import resource="database-context.xml"/>

    <!-- 별도 프로필이 없으면 실제 문자 비용 없이 로컬 발송 흐름을 검증한다. -->
    <beans profile="default,demo">
        <bean id="smsSender"
              class="com.azas.domain.member.service.FakeSmsSender"/>
    </beans>

    <!-- 실제 SMS 발송 환경에서만 SENS 인증정보를 요구한다. -->
    <beans profile="sens">
        <bean id="smsSender"
              class="com.azas.domain.member.service.NaverSensSmsSender">
            <constructor-arg
                    index="0"
                    value="${NCP_SENS_ACCESS_KEY}"/>
            <constructor-arg
                    index="1"
                    value="${NCP_SENS_SECRET_KEY}"/>
            <constructor-arg
                    index="2"
                    value="${NCP_SENS_SERVICE_ID}"/>
            <constructor-arg
                    index="3"
                    value="${NCP_SENS_SENDER_NUMBER}"/>
        </bean>
    </beans>
</beans>
